# Probe evaluation

Curated learner answers with human labels for mastery agreement, evidence-excerpt validity, misconception precision, and overpromotion rate.

False mastery is more costly than underpromotion. Release is blocked while a model can reliably award higher mastery without evidence.

Ordinary CI uses the deterministic mock provider fixtures under `apps/desktop/tests`.
